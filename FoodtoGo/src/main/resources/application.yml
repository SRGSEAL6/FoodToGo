spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/f2go
    username: postgres
    password: Level123!

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  port: 8181
